diff --git a/node_modules/imapflow/lib/handler/imap-stream.js b/node_modules/imapflow/lib/handler/imap-stream.js
index d0b4fda..6e2d9cd 100644
--- a/node_modules/imapflow/lib/handler/imap-stream.js
+++ b/node_modules/imapflow/lib/handler/imap-stream.js
@@ -407,6 +407,26 @@ class ImapStream extends Transform {
         }
     }
 
+    /**
+     * Starts the async input-draining loop under the single-flight guard, and
+     * restarts it if a chunk is queued during teardown. The `processingInput`
+     * flag is reset in a `.finally()` microtask; on workerd a `_transform` call
+     * can land in that window after the loop has already exited, leaving a
+     * queued chunk stranded (the line never completes -> the parser hangs).
+     * Re-draining here guarantees every queued chunk is processed.
+     */
+    _startProcessInput() {
+        this.processingInput = true;
+        this.processInput()
+            .catch(err => this.failStream(err))
+            .finally(() => {
+                this.processingInput = false;
+                if (this.inputQueue.length && !this.destroyed) {
+                    this._startProcessInput();
+                }
+            });
+    }
+
     /**
      * Drains the input queue by processing each queued chunk sequentially.
      * Yields to the event loop every 10 chunks to prevent CPU blocking on
@@ -475,10 +495,7 @@ class ImapStream extends Transform {
         this.inputQueue.push({ chunk, next });
 
         if (!this.processingInput) {
-            this.processingInput = true;
-            this.processInput()
-                .catch(err => this.failStream(err))
-                .finally(() => (this.processingInput = false));
+            this._startProcessInput();
         }
     }
 
diff --git a/node_modules/imapflow/lib/imap-flow.js b/node_modules/imapflow/lib/imap-flow.js
index 9f30530..7059bf9 100644
--- a/node_modules/imapflow/lib/imap-flow.js
+++ b/node_modules/imapflow/lib/imap-flow.js
@@ -1373,15 +1373,25 @@ class ImapFlow extends EventEmitter {
         // The `this.reading` flag acts as a concurrency guard: if reader()
         // is already running, new 'readable' events are ignored. The reader
         // loop will keep draining data until the stream returns null.
+        //
+        // Workerd fires 'readable' earlier than Node (while a previous reader()
+        // is still awaiting handleResponse), so a plain guard drops the event and
+        // stalls. Remember the missed event and re-run after the guard clears.
         this.socketReadable = () => {
-            if (!this.reading) {
-                this.reading = true;
-                this.reader()
-                    .catch(err => this.log.error({ err, cid: this.id }))
-                    .finally(() => {
-                        this.reading = false;
-                    });
+            if (this.reading) {
+                this._readPending = true;
+                return;
             }
+            this.reading = true;
+            this.reader()
+                .catch(err => this.log.error({ err, cid: this.id }))
+                .finally(() => {
+                    this.reading = false;
+                    if (this._readPending) {
+                        this._readPending = false;
+                        this.socketReadable();
+                    }
+                });
         };
 
         this.streamer.on('readable', this.socketReadable);
