# ============================================================
# Copy this file to `.env` for LOCAL development and fill in
# real values. `.env` is git-ignored; NEVER commit real values.
#
# Production secrets are set via `wrangler secret put <NAME>`.
# ============================================================

# 32-byte hex key used to encrypt stored email credentials (AES-GCM).
# Generate:
#   node -e "console.log([...crypto.getRandomValues(new Uint8Array(32))].map(b=>b.toString(16).padStart(2,'0')).join(''))"
CREDENTIAL_ENCRYPTION_KEY=64_hex_characters

# --- Gmail (Google Cloud OAuth) — optional. Leave empty to skip Gmail.
# The OAuth token enables IMAP/SMTP access (XOAUTH2). ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# --- Outlook (Microsoft Entra OAuth) — optional. Leave empty to skip Outlook.
# The OAuth token enables IMAP/SMTP access (XOAUTH2). ---
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=

# --- Browser push (Web Push / VAPID) — optional. Leave empty to skip push. ---
# Generate a keypair:
#   node scripts/generate-vapid.ts
# VAPID_PUBLIC_KEY is public (served to the browser); VAPID_PRIVATE_KEY is a
# secret. VAPID_SUBJECT is a mailto:/https: URI identifying the app.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=

# --- Cloudflare Access JWT verification — OPTIONAL. Leave empty to keep the
# basic header-presence check. To verify each API request's Access JWT, set:
#   ACCESS_JWKS   your application's JWKS (the .../cdn-cgi/access/certs URL)
#   ACCESS_AUD    your application's AUD tag (Application Audience)
ACCESS_JWKS=
ACCESS_AUD=
